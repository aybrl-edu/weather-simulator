exports.getSimulatorParams =  (req, res) => {
    // Check the DATA source {Weather API, Cutom Scenarios}
    // Interogate the data source
    // Build the response object
    // Send it back
    res.end('hello from simulator')
}